<div class="ui large form" id="add_form">
  <div class="ui stacked segment" style="text-align: center;" >
    <h2> we found <%= pluralize(@playlists.size, "playlist") %> that fit this mood</h2>
      <% @playlists.each do |playlist| %>
        <ul>
          <%= link_to(playlist.title, user_playlist_path(playlist.user_id, playlist)) %>
        </ul>
      <% end %>
  </div>
</div>
